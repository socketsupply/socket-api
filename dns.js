/**
 * @module dns
 * @notice This is a rexports of `dns/index.js` so consumers will
 * need to only `import * as fs from '@socketsupply/io/dns.js'`
 */
export * from './dns/index.js'

import * as exports from './dns.js'
export default exports
